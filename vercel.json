{
  "buildCommand": "cd frontend && npm install && CI=false npx react-scripts build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'installed via buildCommand'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
